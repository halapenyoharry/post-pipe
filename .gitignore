# session files and credentials — never commit
auth/

# deps
node_modules/

# generated output
_site/

# feed cache — rebuilt on generate, do not commit
.feed-cache/

# OS
.DS_Store

# content — lives here locally but not in the pipeline repo
articles/
input/
research/
fonts/

# design exploration
design-mock*.html

# test content
test-articles/

# archive (syncthinged to other devices)
archive/

# scratch / binaries kept locally but not in the repo
scrap.md
*.pxd

# library build output — produced by `npm run build:lib`
dist-lib/
node_modules
